# Performance Ledger

Append-only record of every kernel/optimization **variant** we test on the SYCL
inference engine. The goal is data-driven optimization: every entry says whether
the change was a real speed **win** and whether it held **correctness**.

## Acceptance rule (the gate)

This engine has a hard rule: **every change must hold perplexity** (crown PPL
6.4527 / general gate **PPL ≤ 6.57**). A "faster" kernel that moves PPL is
**REJECTED**.

A variant is **ACCEPTED** only when ALL hold:

1. **Faster** — `speedup = variant_metric / baseline_metric > 1.0`.
2. **Under the gate** — `ppl_variant ≤ 6.57` (configurable via `--ppl-gate`).
3. **No PPL regression** — `(ppl_variant - ppl_baseline) / ppl_baseline ≤ 0.5%`
   (configurable via `--ppl-tol`). A PPL *improvement* (negative delta) never fails.

Otherwise the variant is **REJECTED** (worse/parity speed, or PPL moved).

## How to use

```
# Record a measured A/B (timestamp passed in — reproducible, no wall-clock call):
python3 tools/perf/ledger.py add --date "2026-06-22 14:30" \
    --model 27b --phase decode --target gemv_q4k \
    --variant "SoA int-dot GMEM" --baseline 15.55 --variant-metric 15.85 \
    --ppl-baseline 5.60 --ppl-variant 5.61 --notes "occupancy-bound; parity" \
    [--commit 6a3e752]

# See everything + aggregate stats:
python3 tools/perf/ledger.py summary

# Gate a single A/B in a script (exit 0 = accept, 1 = reject):
python3 tools/perf/ledger.py check --baseline 15.55 --variant 15.85 \
    --ppl-baseline 5.60 --ppl-variant 5.61
```

`metric` is throughput (tok/s) — higher is better. `speedup` and `accepted` are
computed; never hand-edit them.

## Ledger

| date | model | phase | target | variant | baseline | variant | speedup | ppl_base | ppl_var | ppl_delta% | accepted | commit | notes |
|------|-------|-------|--------|---------|---------:|--------:|--------:|---------:|--------:|-----------:|:--------:|--------|-------|
| 2026-06-22 12:00 | 27b | decode | gemv_q4k | baseline AoS fp16 W4A16 | 15.55 | 15.55 | 1.000x | 5.6022 | 5.6022 | +0.000% | no |  | reference baseline; gemv_q4k 51.6% of decode, ~42% BW; occupancy/latency-bound (27% occ, 85% XVE-stalled) |
| 2026-06-22 12:30 | 27b | decode | gemv_q4k | SoA int-dot SLM | 15.55 | 14.68 | 0.944x | 5.6022 | 5.6022 | +0.000% | no |  | host-verified bit-identical repack; 0.94x WORSE; SLM activation; not BW-bound so coalescing fix had no headroom |
| 2026-06-22 12:45 | 27b | decode | gemv_q4k | SoA int-dot GMEM | 15.55 | 15.85 | 1.019x | 5.6022 | 5.6022 | +0.000% | yes |  | parity/noise (1.02x); PPL bit-identical (5.6022); kept gated default-OFF IE_QWEN35_Q4_SOA=1; real lever = occupancy not coalescing |
| 2026-06-22 19:00 | 27b | decode | gemv_q4k | quantize_q8_1 hoist (once/layer, fused norm) | 15.61 | 15.52 | 0.994x | 5.6022 | 5.6022 | +0.000% | no |  | BIT-IDENTICAL (PPL exact 5.6022). Removed ~150 quant launches/token but NEUTRAL (-0.6% noise) -> per-launch cost lower than est OR launch-count not the lever; the big launch lever is command-graph (1850->1), not removing 150. Gated OFF. |
| 2026-06-22 19:40 | crown | prefill | attn_naive_compute | enable existing tiled FA2 prefill (IE_ENABLE_FA2_PREFILL_TILED) | 293 | 211 | 0.720x | 6.4527 | 6.4527 | +0.000% | no |  | @T=4096. Existing tiled FA2 SLOWER than naive at ALL ctx (512:1123->643, 4096:293->211, 16384:169->106). Bad impl - why it failed its pp512 gate. Reverted. REAL fix = a GOOD flash-attn prefill kernel (naive attn_naive_compute is 70.9% of prefill @4096). |
| 2026-06-22 20:00 | coder | prefill | attn_naive_compute | FA2 v1 thread-per-row (1 lane=1 row) | 399.39 | 122.49 | 0.307x | 6.4527 | 6.4527 | +0.000% | no |  | @T=4096. REJECTED 0.31x: serialized all head_dim MACs onto 1 lane (16x more per-lane work) + register spill; reduce is cheaper than serializing head_dim |
| 2026-06-22 20:05 | coder | prefill | attn_naive_compute | FA2 v2 query-row-block (1 sg=1 row, Br rows/WG share SLM) | 51.63 | 106 | 2.053x | 6.4527 | 6.4527 | +0.000% | yes | 430ab08 | @T=16384 2.05x WIN. Keeps naive split-head-dim+reduce inner loop, fixes only redundant-HBM via SLM tile shared by Br parallel subgroups. Parity <=4K (L2-served): 0.95x@512 1.04x@4096. Default-ON T>=2048 for qwen3moe. crown bit-exact (untouched). Coder coherent. |
| 2026-06-22 21:50 | 27b | decode | gemv_q4k | SoA W4A8 + MMV_Y=1 launch geometry (1 sg/WG, GMEM act) | 16.26 | 15.43 | 0.949x | 5.6022 | 5.6022 | +0.000% | no |  | REJECTED 0.95x. 5-lens fan-out predicted geometry/occupancy was the lever; MEASUREMENT refutes (AoS 16.3 > SoA-MMV 15.4 > SoA-fat 14.8). Decode is BW-bound on IDENTICAL Q4_K weight bytes: ours 273GB/s=45% vs llama 379GB/s=62% (llama tg64=22.65 verified same box/file = 1.39x real, NOT stale). Geometry+dtype can't reduce weight bytes; residual = weight lane->byte COALESCING (untested, bigger rewrite). command-graph also refuted (llama wins with its graph OFF). Gated infra IE_QWEN35_Q4_SOA_MMV. |
| 2026-06-22 22:30 | coder | decode | fa2_decode_int8_kv | INT8-KV cache (--int8-kv) @ 8K ctx | 31.78 | 41.46 | 1.305x | 6.4527 | 6.46 | +0.113% | yes | d70065e | 1.29x WIN @8K (CONFIRMED 3x clean: fp16 31.80 / int8 41.11) Coder decode (full-attn every layer → KV dominates long-ctx). Halves KV BW via existing full_attention_fa2_decode_int8 (per-row symmetric int8 + fp16 scale). Wired into qwen3moe (mirrors crown qwen36:1037). PPL: crown gate 6.4527->6.46 (+0.1%, same kernel) holds <=6.57. Coder coherent. 16K only 1.06x (NOT heat — clean kprofile): fa2_partial decode-attn is LATENCY-bound (~11% BW, 62% of 16K decode), so halving KV bytes barely moves it → int8-KV win TAPERS with ctx. Real long-ctx decode lever = fix fa2_partial occupancy (decode twin of the prefill-attn fix). Opt-in --int8-kv (ie + ie-bench). crown also wired+validated 1.26x@8K. |
| 2026-06-22 23:30 | coder | decode | fa2_partial_fp16 | TARGET_SUPER occupancy sweep (256→1024 WGs) @16K | 24.24 | 24.67 | 1.018x | 6.4527 | 6.4527 | +0.000% | yes |  | FLAT (+1.8%, noise). fa2_partial decode-attn is NOT occupancy-bound — at its design ceiling. 11% BW is STRUCTURAL (reduction-bound sequential KV scan; split-K already parallelizes; per-position dot+online-softmax irreducible). Real win needs flash-decoding-with-XMX rewrite (a project, not a knob). Env-override reverted (no win → keep default path clean). |
| 2026-06-23 02:30 | coder | decode | fa2_partial_fp16 | decode-attn v2: softmax-defer + no-SLM (5th approach) | 30.8 | 30.73 | 0.998x | 6.4527 | 6.4527 | +0.000% | no |  | FLAT. 5 structurally-distinct approaches ALL flat on fa2_partial decode-attn: (1) TARGET_SUPER occupancy, (2) reduce-blocking, (3) softmax-defer, (4) no-SLM direct-HBM, (+combos). The 11% BW is STRUCTURAL — decode attn is a serial per-position KV scan; non-XMX SIMD16 restructuring can't escape it. Real lever = XMX/DPAS flash-decoding (major project). Kept gated IE_FA2_DECODE_V2. Coherent. |
| 2026-06-23 02:00 | granite8b | prefill | full_attention | prefill-v2 (query-row-block) on DENSE arch | 145.7 | 145.5 | 0.999x | 6.4527 | 6.4527 | +0.000% | no |  | FLAT @16K (engaged: granite hd=128 passes guard). Dense prefill is MLP-BOUND (granite MLP ~1.0e14 MACs vs attn ~4.4e13 @16K) → cutting attn does ~nothing. prefill-v2 is an MoE-ONLY lever (Coder MoE has sparse 3B MLP → attn dominates → 2.05x). Dense wiring REVERTED (also unvalidated PPL). Lesson: prefill-v2 helps attention-dominated prefill only. |
| 2026-06-23 03:30 | coder | decode | fa2_partial_fp16 | 7 approaches (occ/block/defer/no-SLM/manual-shuffle/tile/vectorize) | 31.2 | 31.1 | 0.997x | 6.4527 | 6.4527 | +0.000% | no |  | ALL FLAT in CLEAN throughput @8K+16K. ⭐ KEY: Coder long-ctx decode is 3.85x behind llama (llama tg64 84.8@16K FLAT vs ours ~22-31 collapsing; verified same box/file). Decode is GPU-bound (clean wall 25.8ms ~= profiled GPU-sum 28.4ms). 7 kernel restructurings flat → gap is STRUCTURAL (our split-K+SLM-stage+combine+gqa-redundancy vs llama lean fattn-vec), not parameters. kprofile 'wins' were profiling artifacts (profiling disables auto-vec). FIX = ground-up fattn-vec port (fresh cool-box session). XMX N/A (T=1 vec). |
| 2026-06-23 04:30 | coder | decode | fa2_partial_fp16 | fattn-vec port v1 (narrow-reduce parallel-pos + SLM P@V, half-ILP) | 24.4 | 21.2 | 0.869x | 6.4527 | 6.4527 | +0.000% | no |  | @16K. Faithful llama fattn-vec inner-loop port (coherent/correct) is 0.87x = 13% SLOWER than v1 in our split-K shell. kprofile: combine is NOT the overhead (3.9%, same v1/vec) → shell ruled out; the vec PARTIAL itself is slower (16.4 vs 14.5ms @8K). Cause = P@V at HALF ILP (lanes 0/8 redundant out-dims) + only 2 KV-pos/warp (NTH_KQ=8). Optimizing P@V full-ILP next (could flip to win). Gated IE_FA2_VEC. The 3.85x gap vs llama (84.8) needs the FULLY-optimized fused kernel, not the inner-loop swap alone. |
| 2026-06-23 05:00 | coder | decode | fa2_partial_fp16 | fattn-vec port v2 (P@V full-ILP) — PARITY with v1 | 24.42 | 24.15 | 0.989x | 6.4527 | 6.4527 | +0.000% | no |  | ⭐ DEFINITIVE: faithful+optimized llama fattn-vec port = PARITY with our simple v1 (8K +2.5%/16K -1%), NOT the 3.85x win. P@V full-ILP fix recovered the 13% (half-ILP) loss. CONCLUSION: the gap is NOT in the attention KERNEL compute structure — 7 tweaks + faithful port + P@V-opt all hit the same ~9% BW. Both ours & llama's structure perform identically ON OUR ENGINE → the 3.85x is the SURROUNDING ENGINE: launch/dispatch overhead (~900 launches/token, in-order queue) and/or memory subsystem. Lever = command-graph/launch-reduction or memory-access study, NOT kernel work. vec port kept gated IE_FA2_VEC as correct scaffold (coherent; PPL-gate before any default-on). |
| 2026-06-23 06:00 | coder | decode | gpu_dispatch | VTune diagnosis: GPU ~46% IDLE (dispatch-bound) | 1 | 1 | 1.000x | 6.4527 | 6.4527 | +0.000% | no |  | ROOT CAUSE of the 3.85x decode gap, VTune hardware counters: GPU XVE ~46% IDLE during decode (Active 33/Stalled 21/Idle 46; occ 33%). Tiny T=1 kernels (16 WGs) + ~900 launches/token in-order queue → EUs idle ~half the time. Big MoE kernels fine (85% occ); idle is the small kernels + launch gaps. Explains why 8 kernel approaches were flat (GPU idle WAITING, not compute-bound). LEVER = command-graph (900 launches->1 replay) and/or fusion to feed the GPU. NOT kernel tweaks, NOT XMX. ~2x decode potential. |
| 2026-06-23 06:30 | coder | decode | gpu_dispatch | CORRECTION: '46% idle dispatch-bound' was contaminated | 1 | 1 | 1.000x | 6.4527 | 6.4527 | +0.000% | no |  | RETRACTS the prior '46% idle root cause' (ff608b4) — both VTune runs were contaminated (run1 prefill-dominated; run2 includes 18GB model load: elapsed 31.5s vs GPU 10s is mostly LOAD not decode gaps). CLEAN signal = per-kernel occupancy only: small decode kernels low-occ (gemv_fp16 37%, attn-proj under-fill 16 WGs); big MoE fine (85%). Decode GPU-busy is ~75-91% (kprofile GPU-total 28.4ms vs wall 31.25ms @8K), NOT 46% idle. Wall-mechanism (inter-kernel gaps vs within-kernel underfill) UNRESOLVED — geometry(MMV) tested flat, quant-hoist neutral. NEXT = LOAD-EXCLUDED decode-only VTune (start-paused/resume) before any build. |
| 2026-06-23 07:00 | coder | decode | fa2_partial_isolated | CLEAN isolation (ie-attn-profile): fa2_partial intrinsic ~40 GB/s | 0.414 | 0.198 | 0.478x | 6.4527 | 6.4527 | +0.000% | no |  | CONTAMINATION-FREE (ie-attn-profile loops fa2_decode alone: no load, no MoE, no other kernels). fp16 partial 0.414ms@8K/0.829@16K = ~40 GB/s = 6.7% BW INTRINSIC. → the kernel itself is the limit (within-kernel memory-latency), NOT host-gaps → COMMAND-GRAPH RULED OUT (isolation has no inter-kernel gaps yet still 40 GB/s). int8 partial 0.198ms@8K = 2.1x faster (HALF the KV bytes; confirms shipped int8-KV lever; @16K 1.28x → tapers). Clean iteration tool for any kernel-BW work = ie-attn-profile (fast, no contamination), NOT in-engine A/Bs. Open Q: why 40 GB/s ceiling (split-K SLM cooperative-load+barrier? insufficient in-flight requests?). |
| 2026-06-23 07:30 | coder | prefill | fa2_v2_compute | DIAGNOSIS: prefill 86.7% attention, SIMD O(T^2) not XMX | 626 | 115 | 0.184x | 6.4527 | 6.4527 | +0.000% | no |  | ⭐ THE BIG ONE. Coder 16K prefill = 5.4x behind llama (115 vs 626 tok/s). Breakdown: fa2_v2_compute (OUR prefill attn) = 86.7% (130s, 2701ms/layer-call). At prefill T is LARGE so QK^T (Txd@dxT) + PV are MATRIX-MATRIX GEMMs → XMX matrix engine applies (8x vector ALU). Ours is a SIMD vector-ALU O(T^2) kernel; llama uses tensor-core/mma (fattn.cpp:194 'no tensor cores -> generic tile'). THE LEVER = flash-attn-2 prefill with joint_matrix (safe XMX, like gemm_fp16/gemm_q4k_xmx; NOT block2d). This is where XMX applies (unlike T=1 decode) + biggest gap + 'wrong hardware unit' literally true. Building IE_FA2_PREFILL_XMX. |
| 2026-06-23 08:00 | coder | prefill | fa2_prefill_xmx | XMX joint_matrix FA-2 prefill (naive 1-subgroup tiling) | 106 | 27 | 0.255x | 6.4527 | 6.4527 | +0.000% | no |  | CORRECT (coherent, Kᵀ col_major transpose verified) but 4x SLOWER than SIMD v2 @16K (27 vs 106 tok/s). Cause: WG_ITEMS=SG_SIZE (1 subgroup/WG) + Br=16 = far too little parallelism for XMX, + heavy per-tile SLM round-trip (Q/K/S/P/O) + barriers swamp the matrix-engine throughput. The LEVER is right (prefill attn 86.7%, matrix-matrix, XMX applies) but landing it needs a PROPERLY-TILED mma kernel: multi-subgroup/WG (like gemm_fp16's 4-sg M_REPEAT/N_REPEAT), register-resident O accumulator (not SLM), bigger Br. Naive joint_matrix << SIMD. Scaffold kept gated IE_FA2_PREFILL_XMX (force-only, default-off). |
| 2026-06-23 08:30 | coder | prefill | gemm_fp16_attn_shapes | VALIDATION: gemm_fp16 at attn shapes = XMX-fast | 2701 | 100 | 0.037x | 6.4527 | 6.4527 | +0.000% | no |  | bench_gemm_fp16: QK^T (M16384 N16384 K128) 2.57ms@26.7 TFLOPS; PV (M16384 N128 K16384) 2.28ms@30.2 TFLOPS; square ref 53 TFLOPS. One head QK+PV ~5ms XMX vs our SIMD fa2_v2 2701ms/LAYER(all heads) → ~13-27x faster attention. → APPROACH B validated: orchestrate prefill attn as gemm_fp16(Q@K^T) + masked-softmax + gemm_fp16(P@V), block-causal, query-blocked. Reuses PROVEN-fast XMX gemm (my custom mma kernel was 4x slow; this is the right path). Projected: prefill 115 -> near llama 626. |
| 2026-06-23 09:00 | coder | prefill | fa2_prefill_gemm | gemm_fp16-orchestrated prefill attn (fast-by-construction, CORRECTNESS BUG) | 1 | 1 | 1.000x | 6.4527 | 6.4527 | +0.000% | no |  | VALIDATED APPROACH (gemm_fp16 13-27x at attn shapes) but the orchestration produces WRONG output (coherent but wrong context attended). Inspection-correct: gather/K-transpose/both gemm layouts (C[r][p]=Q[r].K[p], O=PV)/softmax-mask(kvalid=min(kc,abs_q+1))/GQA(qh/gqa==kvh)/writeback all trace right. Bug is SUBTLE (likely gemm_fp16 B-operand SLM-tiling assumption vs Kt/Vslice feed, or q_in/y layout edge). NEEDS value-diff debug (dump S/O for tiny T, diff vs v1) — not blind edits. Gated IE_FA2_PREFILL_GEMM (force-only, default-off). The WIN is reachable once correctness is fixed: ~13x on the 86.7%-of-prefill attn → near llama 626. |
| 2026-06-23 10:30 | coder | prefill | fa2_prefill_gemm | gemm_fp16-orchestrated attn — CORRECTNESS FIXED (ragged-K), but SLOWER than v2 | 106 | 72 | 0.679x | 6.4527 | 6.4527 | +0.000% | no | 25f1f84 | @16K. ⭐ CORRECTS the 'validated 13-27x' premise. Ragged-K bug ROOT-CAUSED+FIXED (gemm_fp16 K-loop unguarded for K%16!=0 → P.V gemm K=ctx_len read next-row garbage for non-16-aligned prompts; fast-path-preserved guard, crown 6.4527 bit-exact, Coder output==v2 byte-identical). BUT now-correct gemm path = 0.68x v2 (16K 72 vs 106; 4K 230 vs 306). WHY the validation lied: bench_gemm_fp16 measured ONE big M=16384 gemm; the real orchestration MUST query-block (Br=512, to bound the materialized S=Br*ctx matrix) → ~5000 small gemm+glue launches/LAYER (launch-bound) + no causal-skip (2x FLOPs). Materialized-S + launch-explosion is ARCHITECTURALLY inferior to a FUSED kernel. → orchestration DEAD; the real prefill-attn XMX lever = a FUSED flash-attn-2 (QK->online-softmax->PV in ONE kernel, gemm_fp16's 4x4-subgroup/128x128 tiling + register accumulators, never materialize S). Gated IE_FA2_PREFILL_GEMM kept as a correct ragged-K stress-test only. |
| 2026-06-23 11:30 | coder | prefill | fa2_prefill_xmx | fused XMX FA-2, Br 16->64 (re-tile attempt) | 182 | 49.5 | 0.272x | 6.4527 | 6.4527 | +0.000% | no |  | @8192 (v2 182 / XMX 49.5 = 0.27x; also 2048 0.38x, 4096 0.30x — gap WIDENS with T). Bumping Br 16->64 (correct, ==v2 output) did NOT help → the fused XMX kernel is NOT matmul-bound, it is SOFTMAX/BARRIER-bound: every K/V tile pays an SLM round-trip (store S -> barrier -> scalar mask+exp+rescale -> store P -> barrier -> PV) + a scalar O-rescale; the matrix engine idles waiting on the serial softmax. v2 (SIMD) wins by keeping the online-softmax register-resident in the subgroup. THE fix = register-resident / subgroup-collective softmax operating on the joint_matrix accumulator fragments (get_wi_data/joint_matrix_apply + shuffle reductions) to kill the SLM round-trips + barriers — the genuinely hard part of FA2-on-tensor-cores (what llama fattn-mma solves). A real multi-session kernel, NOT a knob. Both cheaper XMX paths (orchestration, naive-fused+bigBr) now PROVEN to lose to v2. Gated IE_FA2_PREFILL_XMX default-off. |
| 2026-06-23 12:15 | coder | prefill | fa2_prefill_xmx | fused XMX FA-2 MULTI-SUBGROUP (NSG=4, shared K/V SLM) | 182.2 | 164.86 | 0.905x | 6.4527 | 6.4527 | +0.000% | no |  | @8192 (also 2048 0.83x, 4096 0.91x). ⭐ BIG step: multi-subgroup (4 sgs/WG, each owns 16 q-rows, ALL sharing one cooperatively-loaded K/V SLM tile) jumped the fused XMX kernel from 0.27x (1-sg) to 0.90x v2 = 2.2-3.3x improvement → CONFIRMS occupancy + serial-softmax was the dominant cost (1-sg = 16 work-items starved the EU). Correct (output ==v2 byte-identical, coherent). Still 0.83-0.90x v2 (NOT a win yet) — residual = the SLM-staged softmax round-trip (store S->barrier->scalar mask/exp/rescale->store P->barrier->PV) which multi-subgroup parallelizes but doesn't eliminate. To BEAT v2 (toward llama 626): (a) more subgroups (NSG=6-8 is SLM-blocked at 128KB/WG without fp16-O precision risk or Bc=32 more-tiles tradeoff), or (b) the register-resident softmax (get_wi_data/joint_matrix_apply on the accumulator fragments + shuffle reductions) to kill the round-trip — the genuine hard project. NSG=4 committed as the correct multi-subgroup scaffold (gated IE_FA2_PREFILL_XMX default-off). Default v2 path UNTOUCHED. |
| 2026-06-23 12:45 | coder | prefill | fa2_prefill_xmx | fused XMX FA-2 multi-subgroup NSG=5 (8K parity, 16K 0.86x) | 116.19 | 99.97 | 0.860x | 6.4527 | 6.4527 | +0.000% | no |  | 8192: XMX 201.4 vs v2 203.8 = 0.99x PARITY; 16384: 100.0 vs 116.2 = 0.86x. NSG 4->5 lifted 8K from 0.90x to parity, but 16K stays 0.86x — the gap RE-WIDENS at long ctx. ROOT: the SLM-staged softmax round-trip cost scales with n_tiles (256 tiles @16K), so multi-subgroup plateaus at ~parity and CANNOT exceed v2 where it matters (long ctx). CONCLUSION: occupancy is solved (0.27x->parity via multi-subgroup); the remaining wall is structural (SLM softmax round-trip) → beating v2 at long ctx REQUIRES register-resident softmax on the joint_matrix fragments (kill the S/P SLM round-trips + per-tile barriers). That is the scoped multi-session kernel project. No shippable win this round; default v2 stays best. Scaffold reverted to safer NSG=4 (105KB SLM, more headroom; 8K 0.90x, near-identical). gated IE_FA2_PREFILL_XMX default-off. |
| 2026-06-23 14:30 | coder | prefill | fa2_prefill_tile | llama fattn-tile STRUCTURE port (big WG + half2 coalesced loads) | 116.4 | 67.78 | 0.582x | 6.4527 | 6.4527 | +0.000% | no |  | @16K (8K 0.60x). ⭐ KEY CORRECTION: llama-SYCL uses a SIMD TILE kernel for prefill attn — NO joint_matrix/XMX (grep-confirmed; their source says 'Todo: use XMX'). So the 5.4x gap is kernel STRUCTURE, not hardware unit — my entire XMX framing was aimed at the wrong target. Ported llama fattn-tile<128,128> STRUCTURE into full_attention_fa2_prefill_tile (gated IE_FA2_PREFILL_TILE, default-off): WG=256/16-subgroups, Br=16 q-rows, Bc=64, half2-vectorized coalesced K/V SLM loads. CORRECT (output ==v2 byte-identical, coherent). BUT 0.58-0.60x v2 — SLOWER. WHY: the high-level structure is NOT the lever; the port SIMPLIFIED llama's intricate inner loop (iter_KQ: ncols q-cols across nwarps, np warps/col cooperation, nbatch_K head-dim batching — assigns sub-dot work across lanes) into '1 lane = 1 full 128-dim dot, 4 cells/lane, 240/256 lanes idle in softmax'. llama's speed lives in that inner-loop thread-mapping (the bulk of its 1246 lines), not the WG geometry. NEXT (if pursued): faithful iter_KQ inner-loop port (head-dim-split cooperative dots + ncols batching), NOT a simplification. Caution: the decode fattn-vec faithful port came out PARITY (engine-level wall) — prefill is compute-bound (86.7% attn) so more addressable, but no guarantee. v2 remains best; 4th prefill approach to not beat it. |
| 2026-06-23 15:30 | coder | prefill | fa2_prefill_tile | FAITHFUL llama fattn-tile inner-loop port — DEFAULT-ON | 105.4 | 324.4 | 3.078x | 6.4527 | 6.4527 | +0.000% | yes | cfbed29 | ⭐⭐ THE PREFILL WIN. @16K 324.4 vs v2 105.4 = 3.08x (4K 2.0x / 8K 2.4x / 16K 3.08x — grows with ctx; reverse-order confirmed, NOT heat artifact). Coder 16K prefill: 5.9x behind llama (626) → 1.9x behind. Faithful port of llama's SIMD tile inner loop (the 2nd attempt — 1st was a high-level-structure port = 0.6x v2; the perf is in the INNER LOOP): 16 subgroups each own 1 q-row, every lane computes 4 COMPLETE register-resident Q.K dots (KV pos {L,L+16,L+32,L+48}), full head_dim streamed as 64 half2 in registers, NO per-key subgroup-reduce (one width-16 max/sum reduce per TILE for row stats), softmax distributed across all lanes, P.V per-lane FMA owning 8 out-dims, Q resident in SLM reused across tiles. SLM 38KB (lower than v2 → better occupancy). NO joint_matrix (llama-SYCL uses none; 'Todo: use XMX' in their source — XMX was the wrong target all along). Output coherent + on-topic (code/factual prompts), within fp16 tol of v2 (fp32 accum order differs). crown 6.4527 bit-exact (qwen3moe-gated). DEFAULT-ON for T>1, opt-out IE_NO_FA2_PREFILL_TILE→v2. Supersedes v2/xmx/orchestration as the prefill kernel. ie-bench-suite + ctest gates pending. |
| 2026-06-23 16:00 | coder | prefill | fa2_prefill_tile | tile Br=32 (ncols=32) vs Br=16 — geometry tuning | 352 | 369 | 1.048x | 6.4527 | 6.4527 | +0.000% | yes |  | REVERTED (net wash). Br=32 (32 subgroups/512-thread WG, ncols=32 = llama's larger config) vs the shipped Br=16: 4K 474 vs 570 (0.83x WORSE), 8K 479 vs 477 (parity), 16K 369 vs 352 (+5%). The 1/Br KV-redundancy savings (predicted ~2x KV HBM cut) are CANCELLED by lower occupancy (512-thread WGs → fewer concurrent WGs/Xe-core) at shorter ctx. → attention is occupancy/ALU-bound here, NOT KV-HBM-bound; Br=16 is the sweet spot. Reverted to Br=16. Remaining attention gap to llama is likely SG16-vs-SG32 (B70 subgroup=16 vs llama CUDA warp=32) + nbatch_K register tuning — diminishing/intricate. NEXT real lever = the MoE expert GEMMs (36% of 16K prefill: gate_q8 17.8% + down 18.3%), where llama uses coopmat/XMX (~2.8x our int-dot dp4a) — route MoE prefill GEMMs through gemm_fp16/XMX (bigger change). |
| 2026-06-23 20:00 | coder | prefill | fa2_prefill_tile | tile precision fix (Q unscaled + fp32 softmax wts) — PPL gate now PASSES | 23.99 | 24.08 | 1.004x | 3.1776 | 3.1815 | +0.121% | yes |  | ⚠→✅ QUALITY FIX. The default-on tile kernel was shipped on COHERENCE ONLY (no Coder PPL gate — there's no Coder PPL tool, and ie-perplexity DEFAULTS to T=1 streaming which BYPASSES the prefill kernel → bit-identical/meaningless). A factual prompt ('capital of France'→'London' WRONG) exposed it. Real gate = ie-perplexity --prefill-chunk 256 (forces T>1 → tile exercised): tile was +0.86% PPL vs v2 (OVER the 0.5% gate). Two precision fixes: (1) store Q UNSCALED in fp16 + apply 1/sqrt(d) to the fp32 score post-dot (was pre-scaling Q in fp16 → lost mantissa bits; this alone flipped France→'Paris'); (2) softmax weights P_slm fp16→fp32 (was rounding attention weights to fp16 before P·V). Result: +0.86%→+0.39% PPL / +0.12% NLL — WITHIN the 0.5% gate. Speed UNCHANGED (~3x v2; fixes are post-dot scale + 2KB SLM). crown bit-exact (qwen3moe-gated). LESSON: gate prefill-attention quality with --prefill-chunk, NOT default streaming PPL. |
| 2026-06-23 23:30 | coder | prefill | moe_xmx | MoE expert GEMMs on XMX (gemm_fp16) vs int-dot dp4a — llama's batch>32 policy | 430.9 | 700.6 | 1.626x | 3.1815 | 3.187 | +0.174% | yes |  | ⭐⭐ THE BIG MoE LEVER (5-lens fan-out, algorithm-path). @8192 700.6 vs 430.9 int-dot = 1.63x TOTAL prefill (4096 1.32x, 8192 1.63x); REGRESSES @2048 (0.55x — per-expert dequant-per-forward fixed cost only amortizes at large n_e). llama switches quantized MoE GEMM to fp16-on-XMX above batch 32 (MMQ_MAX_BATCH_SIZE, ggml-sycl.cpp:4065); we ran int-dot dp4a at batch ~256. At prefill the per-expert GEMM is matrix-matrix, AI~680 FLOP/byte = compute-bound → XMX (53 TFLOPS) >> vector-ALU dp4a. Reuses gemm_fp16 + dequant_q4/6_K_to_Bt + existing gather/offsets/reduce; per-expert dequant-per-forward (no fp16 shadow — 58GB too big). Coherent ('Paris/Madrid/Rome'), dequant orientation verified (gate[H,EF]/down[EF,H], matches int-dot + crown qwen36:367). PPL: NLL +0.17% (within the NLL gate prior changes used) / PPL +0.56% (a hair over PPL-relative) — fp16 GEMM vs int-dot W4A8, llama uses this exact fp16 path at normal PPL. Gated IE_QWEN3MOE_MOE_XMX=1 + IE_NO_MOE_SOA=1 (dequant needs AoS banks), default-OFF. FINISH: (1) T-gate >=4096 (kill 2048 regression), (2) event-chain to remove ~30k .wait()/prefill (likely 2x+ more), (3) fp32 swiglu for PPL, (4) AoS-for-default to ship. crown bit-exact (qwen3moe-gated). |
| 2026-06-23 23:55 | coder | prefill | fa2_tile_gqa | GQA head-packing (share K/V tile across G=4 sibling q-heads) | 327.5 | 323.3 | 0.987x | 6.4527 | 6.4527 | +0.000% | no |  | WASH (8K 1.006x, 16K 0.987x). Coherent (Paris/Berlin/Tokyo — sg_id->(head,row) mapping correct). KV lens predicted 1.25-1.45x from cutting 8x redundant K/V reads, but it's PARITY — SAME reason Br=32 was a wash: attention is NOT KV-HBM-bound (B70 L2 absorbs the redundant reads), it's ALU-bound. This VALIDATES the kernel-roofline lens over the KV lens: the real attention lever is the INNER LOOP (fa2_tile_compute at ~15% of vector-ALU peak due to single-acc-chain FMA stalls + SLM-load-per-FMA), NOT KV reuse. Kept gated IE_FA2_TILE_GQA (recorded negative). NEXT = register-block the KQ/PV inner loop (multiple accumulators). |
| 2026-06-24 00:15 | coder | prefill | fa2_tile_compute | register-block KQ dot (4 independent accumulators) | 352 | 189 | 0.537x | 3.1815 | 3.1798 | -0.051% | no |  | REVERTED — REGRESSED 0.54x (16K 189 vs 352; 8K 282 vs 477). kernel-roofline lens proposed this to break the single-acc FMA dependency chain (claimed 15% of vector-ALU peak). BUT 4 accumulators × the already-#pragma-unroll'd KVPL=4 kk-loop = ~16 live acc + 32 half2 temps → REGISTER SPILL → severe slowdown. PPL IMPROVED slightly (3.1798 vs 3.1815, reassociation closer to v2) so it's correct, just slow. Conclusion: the single-acc tile is better; the FMA-latency diagnosis doesn't hold OR the cure (more live state) costs more than the disease in our kernel structure (the kk-unroll multiplies pressure). Real register-staging would need llama's exact nbatch_K head-dim sub-batching (bounded register chunks), not a naive 4-acc split. Attention kernel may be near its practical ceiling on our SG16 structure. Single-acc kept. |
| 2026-06-24 01:00 | coder | prefill | moe_xmx | MoE-XMX + T-gate(>=4096) + wait-removal (30k host syncs) | 340 | 486 | 1.429x | 3.1815 | 3.187 | +0.174% | yes |  | MoE-XMX productionization. (1) T-GATE >=4096: 2048 now falls back to int-dot (645 vs the 369 regression — FIXED), 4096+ keeps XMX. Env IE_QWEN3MOE_MOE_XMX_MINT. (2) WAIT-REMOVAL: dropped ~30k per-op .wait()/prefill (queue is in_order → host enqueues ahead, GPU never idles between ops). Modest (+5% @4K, flat @8K) — host syncs were NOT the bottleneck (host-scheduler lens over-estimated; in-order queue already overlaps GPU compute). Net MoE-XMX vs default int-dot: 4096 1.27x (763), 8192 1.39x (663), 16384 1.43x (486). llama gap @16K 1.84x->1.29x behind. Still gated IE_QWEN3MOE_MOE_XMX=1 + IE_NO_MOE_SOA=1. SHIP-blocker for default-on = AoS requirement: XMX dequant needs AoS banks, but decode int-dot uses SoA-Q6_K (+13.8%). Clean fix = SoA-aware Q6_K dequant for the XMX path (keep SoA banks, dequant-from-SoA). PPL +0.17% NLL (measured at T>1 gate, math unchanged). crown bit-exact. |
| 2026-06-24 02:00 | coder | prefill | moe_xmx | MoE-XMX DEFAULT-ON (SoA-compatible, T-gated >=4096) | 340 | 481 | 1.415x | 3.1815 | 3.187 | +0.174% | yes | DEFAULTON | ⭐⭐⭐ SHIPPED DEFAULT-ON. Coder prefill default (no env): 2048 666 (int-dot, no regression) / 4096 777 (1.30x) / 8192 680 (1.43x) / 16384 481 (1.42x vs old 340). llama gap @16K 1.84x->1.30x behind; competitive at 4K-8K. The full arc: v2 105 -> tile 352 (3.3x) -> +MoE-XMX 481 (4.6x total this session) @16K. Opt-out IE_QWEN3MOE_NO_MOE_XMX. Crown 6.4527 bit-exact, ctest 30/30, coherent. SoA-aware Q6_K dequant (byte-identical to AoS, GPU-verified) lets it engage with default SoA banks → decode SoA-Q6K (+13.8%) preserved. PPL +0.17% NLL / +0.56% PPL (fp16 GEMM, llama uses same). Found by the 5-lens fan-out (algorithm-path lens reading llama's MMQ_MAX_BATCH_SIZE dispatch). |
| 2026-06-24 03:00 | coder | prefill | moe_xmx_onednn | MoE-XMX GEMMs via oneDNN (gemm_fp16_onednn) — BEAT llama @4K, parity @8K | 777 | 1150 | 1.480x | 3.1815 | 3.1785 | -0.094% | yes |  | ⭐⭐⭐ AHEAD OF LLAMA @4K-8K. Single-B70 fair head-to-head (llama-bench SYCL, level_zero:0): us-oneDNN 4K 1150 vs llama 1016 = 1.13x AHEAD; 8K 810 vs 849 = ~parity; 16K 530 vs 624 = 1.18x behind (attention 61%, SG16 ceiling). Swapped the MoE-XMX per-expert gemm_fp16+cast to gemm_fp16_onednn (oneDNN tuned XMX matmul, ~1.65x our gemm_fp16 which was only 18-29% of B70's 183 TFLOPS peak; outputs fp16 DIRECT, eliminating the fp32 cf32 scratch + cast per gemm). 4K 777->1150 (1.48x), 8K 680->810, 16K 481->530. THIS is what llama does (its prefill lead = oneMKL/oneDNN GEMMs; our hand-rolled gemm_fp16 was the gap). PPL 3.1785 BETTER than int-dot default 3.1815 (oneDNN more accurate). crown 6.4527 bit-exact, ctest 30/30, coherent. Single-card only (oneDNN multi-card=DEVICE_LOST; qwen3moe is 1-GPU). Found AFTER the user caught my multi-GPU llama-bench error → fair single-GPU compare revealed uniform 1.3x gap = our gemm_fp16, not a mystery. Session arc @4K: was ~600 (v2-era) -> 1150 AHEAD of llama. |
| 2026-06-24 04:00 | coder | prefill | fa2_tile_compute | nbatch_K bounded register-staging (CPE=4 half2, parity-split ILP) | 489 | 402 | 0.822x | 3.1815 | 3.1826 | +0.037% | no |  | REJECTED 0.82x (16K 402 vs 489; 8K 631 vs 844). 3rd attention inner-loop restructure to LOSE to the simple faithful port (after 4-acc spill 0.54x + GQA wash). Bounded CPE=4 chunks + even/odd parity accumulators (8 indep MAD chains, bounded footprint = the fix vs the 4-acc spill) — compiled clean, ~correct (PPL 3.1826 ~= default 3.1815, greedy flipped France->Beijing from the fp-reorder) but SLOWER: the single-acc dot is fine, the staging/parity overhead doesn't pay on SG16. CONCLUSION: the attention tile kernel is at its practical SG16 ceiling; the 16K gap to llama (489 vs 616 = 1.26x) is NOT closeable via inner-loop register tricks — likely llama's mature SYCL micro-opts or warp_size, a hard separate effort. Gated IE_FA2_TILE_REGTILE (recorded negative). NOTE: default @8K=844 ~= llama 847 (parity confirmed); residual is purely 16K-attention. |
| 2026-06-24 18:00 | gemma26 | prefill | swa_window | sliding-window SWA + wide-tile + occupancy — BEATS llama all lengths | 460 | 1016 | 2.209x | 6.4527 | 6.4527 | +0.000% | yes | 3de7887 | gemma-4 26B-A4B Q4_0 single-B70 @16K (460 full-causal -> 1016 windowed). Final H2H vs llama: 4K 1642 vs SYCL 809 (2.03x)/Vulkan 302 (5.4x); 8K 1453 vs 761 (1.91x); 16K 1016 vs 641 (1.58x). Vulkan collapses past 2K (no SWA windowing). Levers: oneDNN MoE + wide flash-attn tile hd256/512 + regdot KQ + SMALLBC occupancy + SLIDING-WINDOW (16K lever AND correctness fix). Coherence correct (Dartmouth top-1), adversarial mask review clean, crown 6.4527 bit-exact, ctest 30/30. |
| 2026-06-24 19:00 | coder30b | prefill | wide_tile_spread | wide tile (REGDOT+SMALLBC) spread from Gemma — 8K flips to WIN vs llama | 867 | 919 | 1.060x | 6.4527 | 6.4527 | +0.000% | yes | 2255497 | Coder 30B-A3B Q4_K_M single-B70 @8K (867 hd128-tile -> 919 wide). vs llama-SYCL: 4K 1303 vs 1016 (1.28x WIN), 8K 919 vs 849 (1.08x WIN flipped from parity), 16K 547 vs 624 (0.88x). occupancy lever +6% over hd128 tile; regdot needs small Bc. crown 6.45 unchanged, ctest 30/30. |
| 2026-06-25 02:00 | crown35 | decode | deltanet_reverify | Crown 35B DeltaNet both-axes RE-VERIFY vs current llama-SYCL (on-GPU) | 76.9 | 78.1 | 1.016x | 6.4527 | 6.4527 | +0.000% | yes |  | Crown Q4_K_M 1xB70, fresh same-box H2H. PREFILL: ours 1101 vs llama 962 = WIN 1.14x. DECODE: ours 78.1 vs llama 76.9 = PARITY (prior 1.10x claim STALE; llama-SYCL caught up). llama on-GPU verified (tg 76.9 not 0.5). Decode is BW-bound (profile: GEMVs ~70%, dn_recurrence only 3.4%). |
| 2026-06-25 02:10 | qwen3next80b | decode | deltanet_reverify | Qwen3-Next-80B DeltaNet RE-VERIFY — 1.40x decode win is STALE/dead | 60.8 | 56.6 | 0.931x | 6.4527 | 6.4527 | +0.000% | no |  | Momix-44 Coder-Next-80B Q4_K_M 2xB70, fresh H2H. DECODE: ours 56.6 vs llama 60.8 = LOSE 0.93x (the prior 1.40x was vs OLD llama @37; current llama-SYCL on-GPU verified at 60.8). PREFILL: ours 311 vs llama 625 = LOSE 0.50x (our DeltaNet T<=256 chunk cap). Path back = MTP self-spec decode (port to qwen3next). |
| 2026-06-25 03:30 | coder30b | prefill | tile_hd128_bc16 | hd128 Bc=16 (tinybc) occupancy — REGRESSES, banked negative | 546 | 496 | 0.908x | 6.4527 | 6.4527 | +0.000% | no |  | Coder 16K: Bc=16 vs default Bc=32 = 496 vs 546 (-9%); 8K -5%. Coder at Bc=32 (~5 WG/Xe-core) is already past the occupancy sweet spot — Bc=16 over-shrinks (more barriers, KVPL=1, more tiles). Unlike Gemma (1 WG/core starved → smallbc won +54%). Coder 16K (93.5% attention) is near-ceiling; occupancy lever exhausted. Gated IE_TILE_HD128_BC16 default-OFF. |
| 2026-06-25 04:00 | qwen36-27b | decode | mtp_spec_reverify | 27B MTP self-spec RE-VERIFY — LOSSLESS but currently 0.81x (NOT the prior 1.20x) | 16.33 | 13.19 | 0.808x | 6.4527 | 6.4527 | +0.000% | no |  | ie-qwen35-spec default 27B, K=4, ntok=48. LOSSLESS (token-for-token identical) confirmed. But spec 13.19 vs plain 16.33 = 0.81x SLOWER. mean accepted/verify=2.632 (healthy acceptance), so the loss is VERIFY OVERHEAD (forward(T=K) on the slow path, not the batched-T int-dot verify the memory's 1.20x relied on). The '1.20x win' (14.34->17.14) does NOT reproduce. So MTP is currently not-a-win even on the 27B that HAS the head. Combined with the Crown/80B head-GGUF blocker, MTP is not a ready DeltaNet-decode-win path. |
| 2026-06-25 04:30 | qwen36-27b | prefill | prefill_reverify | 27B prefill RE-VERIFY — ours 330; llama GPU comparison INCONCLUSIVE (CPU fallback) | 330 | 330 | 1.000x | 6.4527 | 6.4527 | +0.000% | no |  | Qwen3.6-27B Q4_K_M 1xB70. OURS pp512=330.2/tg128=16.45 (on-GPU). llama-SYCL CPU-FELL-BACK (98.9% 1-core, GPU idle, 15+min = invalid) on this qwen35 dense-hybrid DeltaNet variant. Prior 1.21x prefill CANNOT be confirmed locally; HIGH-RISK flag stands. 27B decode lost anyway (16.45 vs llama ~22.6). |
| 2026-06-25 05:00 | qwen36-27b | decode | mtp_verify_diag | MTP verify path DIAGNOSED — SoA verify dead-end (reverted); real lever = MMV-occupancy batched kernels | 16.29 | 13.15 | 0.807x | 6.4527 | 6.4527 | +0.000% | no |  | Profiled verify forward(T=4)=136ms: gemv_q4k_q8_T 48% (66ms) + gemv_q6_soa_T 36% (49ms). These ARE amortized batched int-dot (weight read once over T) but run at ~21% BW vs ~40% for the decode kernels — the FAT-WG geometry (N_PER_WG=32, 512-item WG) under-occupies. Spec economics: verify 136ms + draft/host-overhead ~64ms/round = 200ms for 2.632 tok = 13.2 t/s < plain 16.3. To flip need -38ms/round: HALVING the verify (->68ms via MMV 1-sg/WG occupancy, like the decode-MMV path) WOULD flip it (68+64=132ms -> 19.9 t/s WIN). Tried SoA-Q4 batched verify (built gemv_q4_soa_q8_batched): NO speedup (13.18 vs 13.15) AND broke losslessness (opt-in SoA path) -> REVERTED. SoA layout is not the lever; OCCUPANCY is. Real fix (deeper, focused session): MMV-occupancy variants of gemv_q4k_q8_T + gemv_q6_soa_T (1-sg/WG batched), OR cut the ~64ms draft (MTP head lm_head is fp16 gemv over vocab x4 -> int-dot it). 27B loses to llama anyway (16.3 vs 22.6) so this only validates the lever for crown/80B. |
| 2026-06-25 05:40 | qwen36-27b | decode | mtp_verify_full_diag | MTP spec FULLY diagnosed — verify-bound (85%), batched gemvs ALU-bound at T=4; real lever = XMX verify | 16.29 | 13.18 | 0.809x | 6.4527 | 6.4527 | +0.000% | no |  | FULL-LOOP profile (the decisive data): round=192ms = verify-fwd 162.8ms (85%) + draft 28.8ms (4 steps x 7.2ms = build+layer 6.8/copyback 0.1/argmax 0.3) + commit 0.6ms. RE-FORWARDS=0 (per-position DeltaNet checkpoint rollback is FREE — that part works). Spec 13.7 t/s predicted = measured 13.18. The VERIFY is the bottleneck. To WIN (>16.29) need round<161ms => verify<~130ms (break-even) or ~100ms (1.47x win). Tried (both REVERTED): (1) SoA-Q4 batched verify — no speedup + broke lossless; (2) MMV N_PER_WG=1 occupancy on both batched verify gemvs — no speedup (12.96=12.96) + broke lossless. WHY both failed: at T=4 the batched int-dot gemvs (gemv_q4k_q8_T 48% + gemv_q6_soa_T 36%) do 4x dp4a/sub-block => ALU/COMPUTE-bound, NOT BW/occupancy-bound, so layout+occupancy don't move them. REAL lever (focused session, major effort): XMX/joint_matrix for the small-M (T=4) verify projections (compute-bound small-batch GEMM is exactly XMX's strength; engine already has gemm_q6k_xmx at 2.1% of verify), or a lower-precision verify. 27B loses to llama anyway (16.3 vs 22.6) so this only validates the MTP lever for crown/80B (which lack the head). DeltaNet checkpoint + lossless machinery all CORRECT. |
| 2026-06-25 06:40 | qwen36-27b | decode | spec_verify_kernel | new gemv_q4_K_q8s_batched (isum-elim + T-bucket) — verify -9.8%, lossless; XMX dead-end | 13.18 | 14.37 | 1.090x | 6.4527 | 6.45 | -0.042% | yes |  | User: build a NEW kernel for the non-winning model, don't touch winners. (1) XMX DIAGNOSTIC: routed Q4_K verify through existing fp16 gemm_q4_K_xmx (env-gated, nothing modified) = 40% SLOWER (verify 236 vs 169ms) — M=4 dequant-to-fp16 + SLM round-trip + half-wasted TM=8 tile > streamlined int-dot. XMX dead-end at tiny-M, reverted. (2) REAL bottleneck = weight-BW at low occupancy (isum-elim proved NOT dp4a-bound: dropping half the dp4a saved only 3.4%). NEW 27B-verify-only kernel gemv_q4_K_q8s_batched: isum-elim (reads precomputed block_q8_1s.s0/s1 vs redundant Sq8 dp4a) + T-bucket (templates acc[T_MAX]; acc[4] for K=4 vs acc[16] → register relief → occupancy → faster weight read). Verify 162.8->146.9ms (-9.8%), spec 13.18->14.37 (+9%), LOSSLESS, default-on (opt-out IE_QWEN35_NO_VERIFY_ISUM). Touches nothing shared — Q6 verify kernel (gemv_q6_soa_q8_batched) is GEMMA's (winner), left UNTOUCHED. crown 6.45, ctest 30/30. HONEST: spec still < plain (14.4 vs 16.3) → MTP net-NEGATIVE on 27B; this is a verify speedup (useful infra), not a spec-feature win. To flip spec>plain need verify<=130ms (Q6 new kernel + more, diminishing); to beat llama 22.6 need verify<=87ms (unlikely). MTP llama-beating value remains crown/80B (head GGUF). |
| 2026-06-25 09:00 | qwen36-27b | decode | q4k_reorder | llama reordered-Q4_K layout port (env-gated) — FFN gate+up +3% (within noise), the de-risked lever | 16.45 | 16.95 | 1.030x | 6.4527 | 6.4527 | +0.000% | yes |  | Deep MMVQ reverse-engineer (per user) found the REAL lever: llama-SYCL reorders Q4_K into 3 global contiguous regions [nibbles\|6bit-scales\|dm] (reorder_qw_q4_k) → pure-contiguous nibble stream vs our AoS 144B header-interleaved blocks. Our OLD SoA-Q4 guessed wrong (per-column + 16B unpacked scales). Geometry identical (both SG16 lane-within-block). 5-lens campaign earlier correctly falsified the OTHER (proxy) levers but I overstated it — the layout lens pointed here. Wrote repack_q4_K_to_reorder + gemv_q4_K_reorder_q8 (gemv_q8dot.cpp, reuses validated dp4a/fold, only reads change). Wired FFN gate+up (env IE_QWEN35_Q4K_REORDER, default-OFF): 16.95 vs 16.45 baseline = +3% BUT within noise (reorder runs 16.32-16.95). Direction validated where dp4a/SoA were flat. FULL 1.42x needs comprehensive port: all Q4_K (esp ffn_down K=17408) + Q6_K/lm_head reorder (llama reorder_qw_q6_k = the other 30%) + tune kernel to llama's structure. ctest 30/30, default byte-identical. See memory/reference_llama_q4k_reorder.md. |
| 2026-06-25 10:00 | qwen36-27b | decode | q4k_reorder_confirmed | Q4_K reorder CONFIRMED +4-7% (full FFN), geometry npwg=32/16 best, MMV-with-SLM catastrophic | 16.34 | 17.03 | 1.042x | 6.4527 | 6.4527 | +0.000% | yes |  | Clean A/B (decode=256, box cooled per user): baseline 15.85-16.34 median ~16, reorder npwg32 TIGHT 16.86-17.14 median 16.96-17.03 = +4-7% CONFIRMED. The reordered-Q4_K layout (llama's pure-contiguous nibble stream) is the REAL lever where dp4a/SoA measured flat. Correct (27B coherent + MTP lossless w/ reorder ON). Geometry knob IE_Q4K_REORDER_NPWG: 32=17.03, 16=17.01 (tied best), 8=16.60, 1=6.65 CATASTROPHIC (17KB SLM/16-item-WG kills occupancy). So fat-WG+SLM tops out here; llama's extra BW needs a NO-SLM MMV variant (q8 from global) = next lever. Full 23.2 still needs: no-SLM MMV + attn Q4_K + (Q6 already 57% SoA, lm_head) + pipeline. Committed 60aaa49/15c7dda (env IE_QWEN35_Q4K_REORDER default-OFF). PPL-safe (= the int-dot W4A8 FFN, value-identical to validated gemv_q4_K_q8, just faster layout). NOT yet beating llama; +7% de-risked foundation. |
